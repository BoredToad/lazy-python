# Lazy evaluation library
This library gives you several classes to deal with lazy evaluation.

`LazyObj` is a simple object

`LazyCollection` is used for iterables,
pair with `InfGenerator` for infinite collections

